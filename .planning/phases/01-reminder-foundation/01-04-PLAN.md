---
phase: 01-reminder-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/features/reminders/form/reminder-form-schema.ts
  - src/features/reminders/form/reminder-form-schema.test.ts
  - src/features/reminders/components/ReminderDrawer.tsx
  - src/features/reminders/components/ReminderDrawer.test.tsx
  - src/features/reminders/components/NextReminderCard.tsx
  - src/features/reminders/components/NextReminderCard.test.tsx
  - src/features/reminders/components/ReminderListSection.tsx
  - src/features/reminders/components/ReminderRow.tsx
  - src/features/reminders/utils/reminder-display.ts
  - src-tauri/src/domain/schedule.rs
  - src-tauri/src/domain/reminder.rs
  - src-tauri/src/commands/reminders.rs
  - src-tauri/src/persistence/reminders.rs
autonomous: true
requirements:
  - SCHD-01
  - SCHD-02
  - REMD-03
must_haves:
  truths:
    - User can choose either interval-based or fixed-time schedules from the same drawer surface.
    - The dashboard and reminder rows always show the next upcoming occurrence computed from the persisted schedule definition.
    - Preset-first schedule editing stays lightweight while advanced controls remain available only when needed.
  artifacts:
    - path: src/features/reminders/components/ReminderDrawer.tsx
      provides: Preset-first interval and fixed-time schedule editor with progressive disclosure.
    - path: src-tauri/src/domain/schedule.rs
      provides: Authoritative next-due calculation for both supported schedule kinds.
    - path: src/features/reminders/components/NextReminderCard.tsx
      provides: User-facing next due read model presentation on the main surface.
    - path: src/features/reminders/components/NextReminderCard.test.tsx
      provides: Automated coverage for next-due rendering after schedule changes.
  key_links:
    - from: src/features/reminders/components/ReminderDrawer.tsx
      to: src/features/reminders/form/reminder-form-schema.ts
      via: mode-specific validation and defaults
      pattern: zodResolver
    - from: src/features/reminders/components/ReminderDrawer.tsx
      to: src-tauri/src/commands/reminders.rs
      via: save payload carries interval or fixed_time schedule
      pattern: kind: "(interval|fixed_time)"
    - from: src-tauri/src/domain/schedule.rs
      to: src/features/reminders/utils/reminder-display.ts
      via: nextDueAt is persisted in Rust and only formatted in the UI
      pattern: nextDueAt
---

<objective>
Finish Phase 1 scheduling by adding the preset-first schedule editor and making next-due information trustworthy for both interval and fixed-time reminders.

Purpose: Satisfy the scheduling requirements without leaking quiet-hours, pause-all, native notification, or history behavior into the foundation phase.
Output: Full schedule editor in the drawer, Rust next-due calculation for both schedule modes, updated dashboard/read-model rendering, and automated tests for schedule validation and next-due presentation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reminder-foundation/01-CONTEXT.md
@.planning/phases/01-reminder-foundation/01-RESEARCH.md
@.planning/phases/01-reminder-foundation/01-UI-SPEC.md
@.planning/phases/01-reminder-foundation/01-VALIDATION.md
@src/features/reminders/form/reminder-form-schema.ts
@src/features/reminders/components/ReminderDrawer.tsx
@src-tauri/src/domain/schedule.rs
@src-tauri/src/commands/reminders.rs

<interfaces>
This plan upgrades the placeholder schedule section from 01-03 into the full Phase 1 editor. The persisted contract remains:

```typescript
type ReminderSchedule =
  | { kind: "interval"; everyMinutes: number; anchorMinuteOfDay: number }
  | { kind: "fixed_time"; weekdays: number[]; times: number[] };
```

Display formatting stays in the UI only; next-due authority stays in Rust. The drawer must expose these preset labels first:

```text
Interval: Every 30 min, Every 45 min, Every 60 min, Custom interval
Fixed time: Weekdays 10:30, Weekdays 15:00, Weekdays 10:30 + 15:00, Custom times
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement authoritative next-due computation for interval and fixed-time schedules</name>
  <files>src-tauri/src/domain/schedule.rs, src-tauri/src/domain/reminder.rs, src-tauri/src/commands/reminders.rs, src-tauri/src/persistence/reminders.rs</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-RESEARCH.md @.planning/phases/01-reminder-foundation/01-VALIDATION.md @.planning/phases/01-reminder-foundation/01-01-PLAN.md @.planning/phases/01-reminder-foundation/01-02-PLAN.md</read_first>
  <behavior>
    - Test 1: an interval schedule computes the next future occurrence from `anchor_minute_of_day` and `every_minutes` relative to now.
    - Test 2: a fixed-time schedule picks the earliest future local weekday and time combination from the selected arrays.
    - Test 3: disabled reminders surface `next_due_at = null` and are omitted from next-up selection.
  </behavior>
  <action>Use chrono in src-tauri/src/domain/schedule.rs to make Rust the sole scheduling authority for Phase 1. For interval schedules, treat `anchor_minute_of_day` as the first minute in the local day from which repeated `every_minutes` offsets are measured, and compute the next timestamp strictly after `now`; if today's remaining slots are exhausted, roll into the next day and continue the same cadence. For fixed-time schedules, search the next 14 local dates, filter by the selected weekday numbers `0..6`, and choose the earliest future local time from the `times` minute-of-day array. Persist `next_due_at` as an RFC3339 string, recompute it on create, update, enable, disable, and list refresh, and keep the repository ordering compatible with enabled-first plus earliest-next-due semantics. Do not add quiet-hours suppression, pause-all semantics, snooze logic, history emission, or background worker loops.</action>
  <verify>
    <automated>cd src-tauri && cargo test computes_next_due</automated>
  </verify>
  <acceptance_criteria>
    - Both supported schedule kinds produce deterministic future timestamps in Rust.
    - The persisted next_due_at field is not authored by React.
    - Disabled reminders stop participating in next-up selection without being deleted.
  </acceptance_criteria>
  <done>The native layer can answer when each reminder is next due for both Phase 1 schedule modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the placeholder timing section with the preset-first schedule editor</name>
  <files>src/features/reminders/form/reminder-form-schema.ts, src/features/reminders/form/reminder-form-schema.test.ts, src/features/reminders/components/ReminderDrawer.tsx, src/features/reminders/components/ReminderDrawer.test.tsx</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-CONTEXT.md @.planning/phases/01-reminder-foundation/01-UI-SPEC.md @.planning/phases/01-reminder-foundation/01-02-PLAN.md @.planning/phases/01-reminder-foundation/01-03-PLAN.md</read_first>
  <behavior>
    - Test 1: choosing an interval preset writes the exact schedule payload expected by the form schema.
    - Test 2: choosing a fixed-time preset writes weekday and minute-of-day arrays without requiring advanced fields.
    - Test 3: expanding advanced options allows custom interval values or custom weekday/time selections and still validates correctly on save.
  </behavior>
  <action>Upgrade the drawer into the full Phase 1 timing editor while preserving the right-side explicit-save flow from 01-03. Add a two-option schedule mode switch only for `interval` and `fixed_time`. For interval mode, render four preset chips labeled `Every 30 min`, `Every 45 min`, `Every 60 min`, and `Custom interval`; the custom path must expand advanced controls for hour/minute duration plus start reference time, and the submitted payload must stay `{ kind: "interval", everyMinutes, anchorMinuteOfDay }`. For fixed-time mode, render four preset chips labeled `Weekdays 10:30`, `Weekdays 15:00`, `Weekdays 10:30 + 15:00`, and `Custom times`; the custom path must expand weekday multi-select plus one-or-more time inputs, and the submitted payload must stay `{ kind: "fixed_time", weekdays, times }`. Keep advanced options collapsed by default. Do not expose quiet hours, pause-all, notification actions, delivery health, history fields, or global settings in this editor.</action>
  <verify>
    <automated>pnpm vitest run src/features/reminders/form/reminder-form-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The drawer supports both Phase 1 schedule modes in one surface.
    - Presets are the first interaction path, with advanced fields hidden until requested.
    - Saved payloads always match the discriminated schedule union defined in 01-02.
  </acceptance_criteria>
  <done>The drawer now satisfies the authoring half of SCHD-01 and SCHD-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface the persisted next-due read model across the hero card and reminder rows</name>
  <files>src/features/reminders/components/NextReminderCard.tsx, src/features/reminders/components/NextReminderCard.test.tsx, src/features/reminders/components/ReminderListSection.tsx, src/features/reminders/components/ReminderRow.tsx, src/features/reminders/utils/reminder-display.ts, src/features/reminders/components/ReminderDrawer.test.tsx</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-UI-SPEC.md @.planning/phases/01-reminder-foundation/01-03-PLAN.md @.planning/phases/01-reminder-foundation/01-04-PLAN.md @.planning/phases/01-reminder-foundation/01-VALIDATION.md</read_first>
  <behavior>
    - Test 1: an interval reminder shows a human-readable next due string in the hero card and row.
    - Test 2: a fixed-time reminder shows a human-readable next due string in the hero card and row.
    - Test 3: a disabled reminder shows `Disabled for now` or `No upcoming time` and never becomes the next reminder card.
  </behavior>
  <action>In src/features/reminders/utils/reminder-display.ts, use date-fns only for formatting persisted timestamps and schedule summaries; do not recompute scheduling logic in the UI. Update NextReminderCard, ReminderRow, and ReminderListSection so they render both schedule summary and next-due text for interval and fixed-time reminders, with the hero card always choosing the earliest enabled reminder that has a nextDueAt value. When a reminder is disabled or has no future time, render the neutral fallback text `No upcoming time` in card and row surfaces instead of blank space. Add tests in NextReminderCard.test.tsx and ReminderDrawer.test.tsx that prove the dashboard updates after schedule edits and that fixed-time reminders can outrank interval reminders when their nextDueAt is earlier.</action>
  <verify>
    <automated>pnpm vitest run src/features/reminders/components/NextReminderCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The main surface exposes next-due information clearly for every reminder.
    - Next reminder selection is driven by the persisted read model rather than placeholder ordering.
    - UI formatting stays presentation-only and does not duplicate the Rust scheduler logic.
  </acceptance_criteria>
  <done>Phase 1 users can see when each reminder is next due after editing, saving, and relaunching the app.</done>
</task>

</tasks>

<verification>
- Run `cd src-tauri && cargo test` after the Rust scheduling work.
- Run `pnpm vitest run src/features/reminders/form/reminder-form-schema.test.ts src/features/reminders/components/ReminderDrawer.test.tsx src/features/reminders/components/NextReminderCard.test.tsx` after the drawer and display work.
- Run `pnpm build` once to confirm the upgraded drawer and dashboard still compile together.
</verification>

<success_criteria>
- QuietBloom supports both interval-based and fixed-time schedules inside the approved drawer flow.
- The main surface shows trustworthy next-due data powered by Rust-owned computation and persisted local state.
- No Phase 2 or Phase 3 features leak into the Phase 1 scheduling surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reminder-foundation/01-04-SUMMARY.md`
</output>
