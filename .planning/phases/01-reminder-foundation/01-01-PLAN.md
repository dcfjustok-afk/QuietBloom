---
phase: 01-reminder-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/test/setup.ts
  - src/test/smoke.test.ts
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - src-tauri/src/lib.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/reminders.rs
  - src-tauri/src/domain/mod.rs
  - src-tauri/src/domain/reminder.rs
  - src-tauri/src/domain/schedule.rs
  - src-tauri/src/persistence/mod.rs
  - src-tauri/src/persistence/reminders.rs
autonomous: true
requirements:
  - REMD-01
  - REMD-03
  - EXPR-03
must_haves:
  truths:
    - After saving reminders and relaunching the app, the native layer can still list those reminders with stable ids and nextDueAt values.
    - Enabled reminders stay visible through one consistent Rust-owned command surface instead of being trapped in transient starter-template UI state.
    - Disabling a saved reminder keeps the row in persistence while removing it from active next-up ordering.
  artifacts:
    - path: src-tauri/src/persistence/reminders.rs
      provides: SQLite-backed repository for reminder CRUD plus next_due_at persistence.
    - path: src-tauri/src/domain/schedule.rs
      provides: Rust schedule enum and authoritative next-due computation entrypoints.
    - path: src-tauri/src/commands/reminders.rs
      provides: Native list, save, delete, and enable or disable command surface.
    - path: src/test/smoke.test.ts
      provides: Wave 0 Vitest baseline proving frontend test infrastructure runs in this repo.
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/reminders.rs
      via: Tauri builder registers reminder commands instead of the greet demo
      pattern: generate_handler!\[.*list_reminders.*save_reminder.*delete_reminder.*set_reminder_enabled
    - from: src-tauri/src/commands/reminders.rs
      to: src-tauri/src/persistence/reminders.rs
      via: command delegates to repository methods for all reminder mutations and reads
      pattern: repository\.(list|save|delete|set_enabled)
    - from: src-tauri/src/persistence/reminders.rs
      to: src-tauri/src/domain/schedule.rs
      via: repository recomputes next_due_at through the domain schedule module
      pattern: compute_next_due
---

<objective>
Establish the native reminder foundation for Phase 1 by replacing the starter repo's demo command with SQLite-backed persistence, authoritative next-due computation hooks, and a minimal Wave 0 frontend test harness.

Purpose: Give later plans a durable storage and command boundary instead of building UI on top of starter-template state.
Output: Installed Phase 1 baseline dependencies, Vitest smoke setup, Rust reminder modules, SQLite schema, and reminder Tauri commands.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reminder-foundation/01-CONTEXT.md
@.planning/phases/01-reminder-foundation/01-RESEARCH.md
@.planning/phases/01-reminder-foundation/01-UI-SPEC.md
@.planning/phases/01-reminder-foundation/01-VALIDATION.md
@package.json
@vite.config.ts
@src-tauri/src/lib.rs
@src-tauri/Cargo.toml
@src-tauri/capabilities/default.json

<interfaces>
Replace the current starter boundary:

```rust
#[tauri::command]
fn greet(name: &str) -> String
```

with a native reminder command surface consisting of exactly these commands:

```rust
list_reminders
save_reminder
delete_reminder
set_reminder_enabled
```

The persisted reminder row must use these concrete columns:

```text
id INTEGER PRIMARY KEY AUTOINCREMENT
type TEXT NOT NULL
title TEXT NOT NULL
description TEXT
enabled INTEGER NOT NULL DEFAULT 1
schedule_kind TEXT NOT NULL
schedule_json TEXT NOT NULL
next_due_at TEXT
created_at TEXT NOT NULL
updated_at TEXT NOT NULL
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wave 0 test harness and native persistence dependencies</name>
  <files>package.json, vite.config.ts, src/test/setup.ts, src/test/smoke.test.ts, src-tauri/Cargo.toml, src-tauri/capabilities/default.json, src-tauri/src/lib.rs</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-RESEARCH.md @.planning/phases/01-reminder-foundation/01-VALIDATION.md @package.json @vite.config.ts @src-tauri/Cargo.toml @src-tauri/capabilities/default.json @src-tauri/src/lib.rs</read_first>
  <action>Add the exact Phase 1 baseline dependencies required by research: add @tauri-apps/plugin-sql, react-hook-form, zod, @hookform/resolvers, and date-fns to package.json dependencies; add vitest, @testing-library/react, @testing-library/user-event, and jsdom to devDependencies; add a concrete `test` script that runs `vitest run`; extend vite.config.ts with a `test` block using `environment: "jsdom"`, `setupFiles: ["./src/test/setup.ts"]`, and an exclude rule for `src-tauri`; create src/test/setup.ts and src/test/smoke.test.ts as the minimal frontend Wave 0 harness; add chrono and tauri-plugin-sql with the `sqlite` feature to src-tauri/Cargo.toml; initialize tauri_plugin_sql in src-tauri/src/lib.rs next to opener; and extend src-tauri/capabilities/default.json with `sql:default`. Do not add store, notification, autostart, log, quiet-hours, history, or settings dependencies in this task.</action>
  <verify>
    <automated>pnpm vitest run src/test/smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains a non-watch `test` script and the approved Phase 1 libraries only.
    - vite.config.ts can execute Vitest in jsdom without requiring a browser or Tauri runtime.
    - src-tauri/src/lib.rs initializes both opener and sql plugins.
    - The capability file grants SQL access without adding unrelated plugin scopes.
  </acceptance_criteria>
  <done>Wave 0 exists and later plans no longer need to invent test infrastructure or database wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Rust reminder schema, repository, and command boundary</name>
  <files>src-tauri/src/commands/mod.rs, src-tauri/src/commands/reminders.rs, src-tauri/src/domain/mod.rs, src-tauri/src/domain/reminder.rs, src-tauri/src/domain/schedule.rs, src-tauri/src/persistence/mod.rs, src-tauri/src/persistence/reminders.rs, src-tauri/src/lib.rs</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-CONTEXT.md @.planning/phases/01-reminder-foundation/01-RESEARCH.md @src-tauri/src/lib.rs @src-tauri/Cargo.toml</read_first>
  <action>Split src-tauri/src/lib.rs into commands, domain, and persistence modules. In src-tauri/src/persistence/reminders.rs, bootstrap a single `reminders` table with the exact columns listed in this plan and add an index on `(enabled, next_due_at)`. In src-tauri/src/domain/reminder.rs and src-tauri/src/domain/schedule.rs, define serde-serializable reminder and schedule types where schedule `kind` is either `interval` or `fixed_time`, interval uses `every_minutes` and `anchor_minute_of_day`, fixed-time uses `weekdays` and `times`, empty titles are rejected, empty weekday arrays are rejected, empty time arrays are rejected, and interval values outside 5..=1440 are rejected. In src-tauri/src/commands/reminders.rs, expose exactly four commands named `list_reminders`, `save_reminder`, `delete_reminder`, and `set_reminder_enabled`, and make save, toggle, and list recompute `next_due_at` before returning DTOs. Do not add quiet-hours columns, pause_until fields, notification metadata, history tables, or app settings storage.</action>
  <verify>
    <automated>cd src-tauri && cargo test persistence::reminders::tests::persists_and_reloads && cargo test reminder_repository</automated>
  </verify>
  <acceptance_criteria>
    - The starter `greet` command is removed from the app domain boundary.
    - The repository can create, list, update, delete, and enable or disable reminders through SQLite.
    - `schedule_json` is the only schedule persistence payload and `next_due_at` is stored on the reminder row.
    - Rust is the authoritative writer for validation and next-due recomputation.
    - A persistence reload test proves saved reminders are still listed after reopening the repository state.
  </acceptance_criteria>
  <done>The native reminder foundation exists and can serve real reminder data to the frontend.</done>
</task>

</tasks>

<verification>
- Run `pnpm vitest run src/test/smoke.test.ts` after the Wave 0 setup.
- Run `cd src-tauri && cargo test` after the Rust repository and command work.
- Run `pnpm build` once to confirm the repo still compiles after native reminder modules are introduced.
</verification>

<success_criteria>
- QuietBloom no longer stores reminder intent in transient starter-template state.
- The codebase has a minimal but durable reminder schema with SQLite persistence and next_due_at support.
- Later Phase 1 plans can build UI and typed frontend contracts on top of a stable native surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reminder-foundation/01-01-SUMMARY.md`
</output>