---
phase: 01-reminder-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/features/reminders/model/reminder.ts
  - src/features/reminders/form/reminder-form-schema.ts
  - src/features/reminders/form/reminder-form-schema.test.ts
  - src/features/reminders/api/reminders.ts
autonomous: true
requirements:
  - REMD-01
  - REMD-03
  - SCHD-01
  - SCHD-02
must_haves:
  truths:
    - React reminder code uses one shared typed contract for reminder types, schedules, and save payloads instead of ad hoc object literals.
    - Invalid interval and fixed-time payloads are rejected before they cross the Tauri boundary.
    - The frontend can call list, save, delete, and enable or disable reminder commands through typed wrappers rather than raw invoke strings inside UI components.
  artifacts:
    - path: src/features/reminders/model/reminder.ts
      provides: Shared TypeScript reminder DTOs and schedule unions.
    - path: src/features/reminders/form/reminder-form-schema.ts
      provides: Zod schema and default values for reminder authoring.
    - path: src/features/reminders/form/reminder-form-schema.test.ts
      provides: Automated schema coverage for interval and fixed-time payloads.
    - path: src/features/reminders/api/reminders.ts
      provides: Typed invoke wrappers for reminder commands.
  key_links:
    - from: src/features/reminders/form/reminder-form-schema.ts
      to: src/features/reminders/model/reminder.ts
      via: frontend schema mirrors the shared reminder schedule contract
      pattern: z\.discriminatedUnion\("kind"
    - from: src/features/reminders/api/reminders.ts
      to: src-tauri/src/commands/reminders.rs
      via: typed wrappers call the exact native command names
      pattern: invoke\("(list_reminders|save_reminder|delete_reminder|set_reminder_enabled)"
---

<objective>
Mirror the native reminder surface in TypeScript so every later UI task consumes one stable contract, schema, and API wrapper layer.

Purpose: Prevent the dashboard and drawer plans from inventing schedule payloads or raw invoke calls while building Phase 1 UI.
Output: Shared TypeScript reminder types, Zod schemas and defaults, schema tests, and typed reminder invoke wrappers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reminder-foundation/01-CONTEXT.md
@.planning/phases/01-reminder-foundation/01-RESEARCH.md
@.planning/phases/01-reminder-foundation/01-UI-SPEC.md
@src/features/reminders/model/reminder.ts
@src-tauri/src/commands/reminders.rs

<interfaces>
Create these exact frontend contract shapes:

```typescript
export type ReminderType = "hydration" | "standing" | "stretching" | "eye_rest" | "custom";

export type ReminderSchedule =
  | { kind: "interval"; everyMinutes: number; anchorMinuteOfDay: number }
  | { kind: "fixed_time"; weekdays: number[]; times: number[] };

export type ReminderSummary = {
  id: number;
  type: ReminderType;
  title: string;
  description: string | null;
  enabled: boolean;
  schedule: ReminderSchedule;
  scheduleSummary: string;
  nextDueAt: string | null;
};

export type SaveReminderInput = {
  id?: number;
  type: ReminderType;
  title: string;
  description: string;
  enabled: boolean;
  schedule: ReminderSchedule;
};
```

Use this exact default schedule for new reminders in later plans:

```typescript
{ kind: "interval", everyMinutes: 60, anchorMinuteOfDay: 540 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared reminder contracts and schema defaults</name>
  <files>src/features/reminders/model/reminder.ts, src/features/reminders/form/reminder-form-schema.ts, src/features/reminders/form/reminder-form-schema.test.ts</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-RESEARCH.md @.planning/phases/01-reminder-foundation/01-01-PLAN.md @.planning/phases/01-reminder-foundation/01-VALIDATION.md</read_first>
  <action>In src/features/reminders/model/reminder.ts, define the exact ReminderType, ReminderSchedule, ReminderSummary, and SaveReminderInput shapes described in this plan and mirror the Rust command DTO field names exactly. In src/features/reminders/form/reminder-form-schema.ts, build a Zod discriminated union over `kind` for `interval` and `fixed_time`, reject empty titles, reject empty weekday arrays, reject empty time arrays, reject `everyMinutes` values outside 5..=1440, and export the concrete default create payload `{ kind: "interval", everyMinutes: 60, anchorMinuteOfDay: 540 }`. In src/features/reminders/form/reminder-form-schema.test.ts, add tests for valid interval payloads, valid fixed-time payloads, empty-title rejection, empty-weekday rejection, empty-time rejection, and out-of-range interval rejection.</action>
  <verify>
    <automated>pnpm vitest run src/features/reminders/form/reminder-form-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Reminder contracts are defined once and reused rather than restated in UI components.
    - Schema tests cover both supported schedule kinds and the key invalid payloads.
    - The exported default reminder payload uses the exact agreed Phase 1 interval schedule.
  </acceptance_criteria>
  <done>The frontend has one stable, test-backed contract layer for reminder authoring and listing.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed invoke wrappers for the native reminder command surface</name>
  <files>src/features/reminders/api/reminders.ts</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-01-PLAN.md @.planning/phases/01-reminder-foundation/01-RESEARCH.md @.planning/phases/01-reminder-foundation/01-VALIDATION.md</read_first>
  <action>Create src/features/reminders/api/reminders.ts with exactly four typed wrappers named `listReminders`, `saveReminder`, `deleteReminder`, and `setReminderEnabled`. Each wrapper must call the matching native command name `list_reminders`, `save_reminder`, `delete_reminder`, or `set_reminder_enabled` via `invoke` from `@tauri-apps/api/core`, return typed Promise results using the shared reminder contracts, and avoid embedding any UI-specific state or formatting logic. Do not add event listeners, query libraries, browser storage fallbacks, or extra command wrappers in this task.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The API layer exposes exactly four reminder operations and no more.
    - The wrapper signatures use the shared reminder contracts from src/features/reminders/model/reminder.ts.
    - No UI component needs to hard-code native command names after this task.
  </acceptance_criteria>
  <done>Later Phase 1 UI plans can consume typed API helpers instead of raw invoke strings.</done>
</task>

</tasks>

<verification>
- Run `pnpm vitest run src/features/reminders/form/reminder-form-schema.test.ts` after schema work.
- Run `pnpm build` to confirm the new contract and API modules compile cleanly.
</verification>

<success_criteria>
- Frontend reminder authoring and list rendering are anchored to one shared contract layer.
- Invalid schedule payloads are blocked before they reach Rust commands.
- Phase 1 UI work can proceed without inventing ad hoc DTOs or raw invoke strings.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reminder-foundation/01-02-SUMMARY.md`
</output>