---
phase: 01-reminder-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/App.tsx
  - src/App.css
  - src/app/AppShell.tsx
  - src/features/reminders/components/NextReminderCard.tsx
  - src/features/reminders/components/TodayOverviewCard.tsx
  - src/features/reminders/components/ReminderListSection.tsx
  - src/features/reminders/components/ReminderRow.tsx
  - src/features/reminders/components/ReminderDrawer.tsx
  - src/features/reminders/components/ReminderDrawer.test.tsx
  - src/features/reminders/components/ReminderRow.test.tsx
  - src/features/reminders/utils/reminder-display.ts
autonomous: true
requirements:
  - REMD-01
  - REMD-02
  - REMD-03
must_haves:
  truths:
    - User can add, edit, enable or disable, and delete reminders from the main desktop surface without leaving the dashboard.
    - The first screen answers what is next and what the current reminder set looks like, instead of rendering the starter greet form.
    - Drawer save flows are explicit and preserve orientation because the dashboard remains visible behind the right-side editor.
  artifacts:
    - path: src/app/AppShell.tsx
      provides: Dashboard-first shell with hero cards and reminder management section.
    - path: src/features/reminders/components/ReminderDrawer.tsx
      provides: Right-side create and edit drawer with explicit save and unsaved-change handling.
    - path: src/features/reminders/components/ReminderRow.tsx
      provides: Per-reminder edit, toggle, and delete actions.
    - path: src/features/reminders/components/ReminderDrawer.test.tsx
      provides: Automated create and unsaved-change coverage for the drawer flow.
  key_links:
    - from: src/App.tsx
      to: src/app/AppShell.tsx
      via: starter screen replaced by reminder shell
      pattern: <AppShell
    - from: src/app/AppShell.tsx
      to: src/features/reminders/api/reminders.ts
      via: app shell loads and refreshes a shared reminders dataset through listReminders
      pattern: listReminders\(
    - from: src/app/AppShell.tsx
      to: src/features/reminders/components/NextReminderCard.tsx
      via: hero card receives next reminder data from the same dataset that drives the list
      pattern: <NextReminderCard
    - from: src/app/AppShell.tsx
      to: src/features/reminders/components/TodayOverviewCard.tsx
      via: overview card derives counts from the same dataset that drives the list
      pattern: <TodayOverviewCard
    - from: src/app/AppShell.tsx
      to: src/features/reminders/components/ReminderListSection.tsx
      via: reminder list receives the shared reminders dataset and mutation callbacks
      pattern: <ReminderListSection
    - from: src/features/reminders/components/ReminderDrawer.tsx
      to: src/features/reminders/api/reminders.ts
      via: drawer save flow persists through saveReminder before the shared dataset refreshes
      pattern: saveReminder
---

<objective>
Replace the starter template UI with the approved dashboard-first shell and ship the core reminder CRUD flows on top of the contracts from 01-01 and 01-02.

Purpose: Deliver the first usable desktop surface for reminder management while keeping full schedule editing deferred to 01-04.
Output: App shell, hero cards, reminder list, right-side reminder drawer, CRUD mutations, and component tests for create/edit/toggle/delete behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reminder-foundation/01-CONTEXT.md
@.planning/phases/01-reminder-foundation/01-RESEARCH.md
@.planning/phases/01-reminder-foundation/01-UI-SPEC.md
@.planning/phases/01-reminder-foundation/01-VALIDATION.md
@src/App.tsx
@src/App.css
@src/main.tsx

<interfaces>
Consume the reminder contracts created in 01-02 exactly as written.

The create-flow default schedule for this plan is fixed to:

```typescript
{ kind: "interval", everyMinutes: 60, anchorMinuteOfDay: 540 }
```

This plan must not expose the full schedule editor yet; 01-04 replaces the placeholder timing section with the preset-first editor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the starter screen with the dashboard shell and reminder overview surfaces</name>
  <files>src/App.tsx, src/App.css, src/app/AppShell.tsx, src/features/reminders/components/NextReminderCard.tsx, src/features/reminders/components/TodayOverviewCard.tsx, src/features/reminders/components/ReminderListSection.tsx, src/features/reminders/components/ReminderRow.tsx, src/features/reminders/utils/reminder-display.ts</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-CONTEXT.md @.planning/phases/01-reminder-foundation/01-UI-SPEC.md @src/App.tsx @src/App.css</read_first>
  <action>Delete the entire greet demo flow from src/App.tsx and replace it with an AppShell-centered reminder dashboard. The shell must implement the UI-SPEC structure exactly enough to guide later work: top bar with the single primary CTA `New reminder`, a hero row where NextReminderCard occupies roughly two-thirds width and TodayOverviewCard occupies roughly one-third, and a `Your reminders` section rendered as card-like rows rather than a table. In src/App.css, establish the CSS token layer using the approved colors `#F4F1EA`, `#E6E0D5`, `#6A8472`, and `#A65F52` and remove the starter Inter/dark-mode template styles. Sort rows with enabled reminders first, then by nearest `nextDueAt`, then disabled reminders. Do not add navigation rails, routing, charts, history panels, notification banners, pause-all controls, or settings surfaces.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The first screen is dashboard-first rather than starter-template-like.
    - The starter logos, greet form, and template copy are completely removed.
    - The reminder list uses cards rather than a dense admin table.
  </acceptance_criteria>
  <done>The desktop shell matches the Phase 1 information hierarchy and can host reminder CRUD actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the right-side reminder drawer with explicit save and CRUD mutations</name>
  <files>src/features/reminders/components/ReminderDrawer.tsx, src/features/reminders/components/ReminderDrawer.test.tsx, src/features/reminders/components/ReminderRow.tsx, src/features/reminders/components/ReminderRow.test.tsx, src/app/AppShell.tsx</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-CONTEXT.md @.planning/phases/01-reminder-foundation/01-UI-SPEC.md @.planning/phases/01-reminder-foundation/01-02-PLAN.md @src/App.tsx @src/App.css</read_first>
  <behavior>
    - Test 1: opening `New reminder`, filling type, title, and description, then pressing save submits one create mutation with the default interval schedule.
    - Test 2: closing a dirty drawer shows the unsaved-change prompt with continue editing, discard, and save options.
    - Test 3: editing an existing reminder reuses the same drawer and preserves its id for update.
  </behavior>
  <action>Create a fixed-width right-side drawer in src/features/reminders/components/ReminderDrawer.tsx that keeps the dashboard visible behind it and uses explicit save in a sticky footer. The drawer must include the Phase 1 content fields in this exact order: reminder type selector, title, description, a read-only timing summary section, and enabled-on-save control. Until 01-04 lands, new reminders must be created with the concrete default schedule `{ kind: "interval", everyMinutes: 60, anchorMinuteOfDay: 540 }` and the drawer should display the summary text `Every 60 minutes starting at 09:00` rather than pretending the schedule editor already exists. Wire create and update through saveReminder, and wire row edit actions to reopen the same drawer with existing values. Implement the unsaved-change confirmation exactly as the UI-SPEC describes. Do not add modal-based editing, auto-save, fixed-time controls, advanced schedule fields, or any pause/quiet-hours fields in this plan.</action>
  <verify>
    <automated>pnpm vitest run src/features/reminders/components/ReminderDrawer.test.tsx -t "creates reminder"</automated>
  </verify>
  <acceptance_criteria>
    - Reminder creation is possible before the full schedule editor exists because the drawer uses the agreed default schedule payload.
    - Edit flows reuse the drawer instead of branching to a separate page or modal.
    - Unsaved changes are never lost silently.
  </acceptance_criteria>
  <done>User can create and edit reminders from the main surface without context switching away from the dashboard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire list refresh, toggle, and delete flows into the shared dashboard dataset</name>
  <files>src/app/AppShell.tsx, src/features/reminders/components/ReminderRow.tsx, src/features/reminders/components/ReminderRow.test.tsx, src/features/reminders/components/ReminderListSection.tsx, src/features/reminders/components/NextReminderCard.tsx</files>
  <read_first>@.planning/phases/01-reminder-foundation/01-VALIDATION.md @.planning/phases/01-reminder-foundation/01-02-PLAN.md @.planning/phases/01-reminder-foundation/01-03-PLAN.md @src/App.tsx @src/App.css</read_first>
  <behavior>
    - Test 1: toggling a reminder calls setReminderEnabled and refreshes the list state.
    - Test 2: deleting a reminder requires confirmation and removes the row after deleteReminder resolves.
    - Test 3: the hero card updates when the earliest active reminder changes.
  </behavior>
  <action>In AppShell, load reminders on mount through listReminders and keep the dashboard data in local feature state; do not introduce a global state library. Implement per-row enable and disable using setReminderEnabled, delete confirmation using deleteReminder, and list refresh after every mutation so NextReminderCard, TodayOverviewCard, and ReminderListSection always reflect the same reminders collection. Make disabled reminders visible with reduced contrast instead of filtering them out. Add component tests that cover toggle and delete flows in ReminderRow.test.tsx and keep the hero card driven by the same refreshed dataset rather than independent mocked state. Do not add toast-only feedback, browser storage fallbacks, or optimistic ghost rows that bypass the native command boundary.</action>
  <verify>
    <automated>pnpm vitest run src/features/reminders/components/ReminderRow.test.tsx -t "edits toggles deletes"</automated>
  </verify>
  <acceptance_criteria>
    - The main surface supports create, edit, enable or disable, and delete from one place.
    - Hero content and list content come from the same reminder dataset.
    - Disabled reminders remain visible and understandable rather than disappearing.
  </acceptance_criteria>
  <done>QuietBloom has a usable reminder CRUD dashboard that satisfies the management half of Phase 1.</done>
</task>

</tasks>

<verification>
- Run `pnpm vitest run src/features/reminders/components/ReminderDrawer.test.tsx src/features/reminders/components/ReminderRow.test.tsx` after CRUD wiring is complete.
- Run `pnpm build` to confirm the starter replacement compiles cleanly.
- Manual visual review can happen after execution, but this plan contains no blocking human checkpoint.
</verification>

<success_criteria>
- Users can manage multiple reminders from a dashboard-first desktop surface.
- Editing occurs in a right-side explicit-save drawer with preserved context behind it.
- The app no longer resembles the default Tauri template and does not leak Phase 2 or Phase 3 controls into Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reminder-foundation/01-03-SUMMARY.md`
</output>