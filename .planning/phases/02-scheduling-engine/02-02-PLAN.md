---
phase: 02-scheduling-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src-tauri/src/commands/scheduler.rs
  - src-tauri/src/lib.rs
  - src/features/reminders/model/scheduler.ts
  - src/features/reminders/api/scheduler.ts
  - src/features/reminders/form/reminder-form-schema.ts
  - src/app/AppShell.tsx
  - src/App.css
  - src/features/reminders/components/SchedulerControlStrip.tsx
  - src/features/reminders/components/SchedulerControlStrip.test.tsx
  - src/features/reminders/components/ReminderDrawer.tsx
autonomous: true
requirements:
  - SCHD-03
  - SCHD-04
must_haves:
  truths:
    - User can edit quiet hours from the dashboard-top control strip and save them explicitly without leaving the dashboard shell.
    - User can pause all reminders using exactly four presets and resume early from the same strip.
    - User can define reminder-level allowed hours inside the existing drawer while recovery-driven hero or row status changes remain deferred to the lifecycle hardening plan.
  artifacts:
    - path: src/features/reminders/components/SchedulerControlStrip.tsx
      provides: Shared dashboard-top control strip, including quiet-hours and pause-all subregions.
    - path: src/features/reminders/model/scheduler.ts
      provides: Typed frontend contract for scheduler snapshot, quiet hours, and pause-all commands.
    - path: src/features/reminders/components/ReminderDrawer.tsx
      provides: Active-window editor inside the existing advanced schedule area.
    - path: src-tauri/src/commands/scheduler.rs
      provides: Tauri command boundary for scheduler snapshot, quiet-hours save, pause-all, and resume.
  key_links:
    - from: src/app/AppShell.tsx
      to: src/features/reminders/api/scheduler.ts
      via: dashboard control strip fetches and refreshes app-wide scheduler snapshot
      pattern: scheduler
    - from: src/features/reminders/components/ReminderDrawer.tsx
      to: src/features/reminders/form/reminder-form-schema.ts
      via: allowed-hours fields validate against the shared schedule union
      pattern: activeWindow
    - from: src-tauri/src/commands/scheduler.rs
      to: src-tauri/src/persistence/scheduler_state.rs
      via: native commands persist quiet-hours and pause-all policy before runtime invalidation
      pattern: scheduler_state
---

<objective>
Add quiet-hours, pause-all, and next-fire recalculation rules through the approved dashboard strip and drawer surfaces.

Purpose: Expose Phase 2 scheduler policy to users without creating a settings page, a second scheduling system, or delivery-era interaction surfaces.
Output: Typed scheduler command and API boundary, dashboard control strip for app-wide timing policy, and reminder drawer support for reminder-level allowed hours with read-model updates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scheduling-engine/02-CONTEXT.md
@.planning/phases/02-scheduling-engine/02-UI-SPEC.md
@.planning/phases/02-scheduling-engine/02-RESEARCH.md
@.planning/phases/02-scheduling-engine/02-VALIDATION.md
@.planning/phases/02-scheduling-engine/02-01-PLAN.md
@src/app/AppShell.tsx
@src/features/reminders/components/ReminderDrawer.tsx
@src/features/reminders/model/reminder.ts
@src/features/reminders/api/reminders.ts
@src/features/reminders/utils/reminder-display.ts

<interfaces>
Phase 1 currently exposes only reminder CRUD plus this shared schedule contract:

```typescript
export type ReminderSchedule =
  | { kind: "interval"; everyMinutes: number; anchorMinuteOfDay: number }
  | { kind: "fixed_time"; weekdays: number[]; times: number[] };
```

This plan must extend the frontend boundary with typed scheduler snapshot commands and a reminder schedule shape that can carry `activeWindow`, while keeping the approved UI surfaces limited to:

```text
Dashboard-top scheduler control strip
Reminder drawer advanced schedule area
```

No settings page, notification controls, history timeline, snooze, skip, or complete actions are allowed here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the typed scheduler boundary for quiet hours, pause-all, and allowed-window schedules</name>
  <files>src-tauri/src/commands/mod.rs, src-tauri/src/commands/scheduler.rs, src-tauri/src/lib.rs, src/features/reminders/model/scheduler.ts, src/features/reminders/api/scheduler.ts, src/features/reminders/form/reminder-form-schema.ts, src/features/reminders/form/reminder-form-schema.test.ts</files>
  <read_first>@.planning/phases/02-scheduling-engine/02-RESEARCH.md @.planning/phases/02-scheduling-engine/02-VALIDATION.md @.planning/phases/02-scheduling-engine/02-01-PLAN.md @src/features/reminders/model/reminder.ts @src/features/reminders/api/reminders.ts</read_first>
  <behavior>
    - Test 1: the shared reminder schema accepts an optional reminder-level active window while preserving existing interval and fixed-time authoring defaults.
    - Test 2: the frontend has a typed scheduler snapshot contract for quiet hours, pause-all state, and runtime summary copy.
    - Test 3: native scheduler commands expose only Phase 2 timing controls and do not leak delivery or history actions.
  </behavior>
  <action>Extend the typed scheduler boundary so React can fetch the scheduler snapshot, save quiet hours, start pause-all using the four approved presets, and resume early. Create src-tauri/src/commands/scheduler.rs and register it from src-tauri/src/lib.rs, then mirror only the app-wide scheduler contracts in src/features/reminders/model/scheduler.ts and src/features/reminders/api/scheduler.ts. Update reminder form schema validation just enough to accept reminder-level active-window inputs as part of the existing schedule payload instead of creating a second frontend-only timing structure. Keep the boundary intentionally narrow: no notification delivery commands, permission health, settings-page preferences, history ledger access, or snooze/skip/complete actions.</action>
  <verify>
    <automated>pnpm vitest run src/features/reminders/form/reminder-form-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - React has typed command wrappers for quiet hours, pause-all, and scheduler snapshot reads.
    - Reminder form validation can express active-window data without breaking Phase 1 schedules.
    - The native command surface remains limited to Phase 2 timing policy.
  </acceptance_criteria>
  <done>The frontend and native scheduler boundaries are aligned for Phase 2 control surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the dashboard-top scheduler control strip for quiet hours and pause-all</name>
  <files>src/app/AppShell.tsx, src/App.css, src/features/reminders/components/SchedulerControlStrip.tsx, src/features/reminders/components/SchedulerControlStrip.test.tsx, src/features/reminders/components/TodayOverviewCard.tsx</files>
  <read_first>@.planning/phases/02-scheduling-engine/02-UI-SPEC.md @.planning/phases/02-scheduling-engine/02-VALIDATION.md @src/app/AppShell.tsx @src/features/reminders/components/TodayOverviewCard.tsx</read_first>
  <behavior>
    - Test 1: the control strip renders under the existing top bar and exposes quiet-hours summary plus pause-all status without adding a new route.
    - Test 2: pause-all offers exactly four presets: 30 minutes, 1 hour, 2 hours, and rest of today.
    - Test 3: the strip uses explicit-save quiet-hours editing and lightweight runtime state copy instead of a settings-panel or admin-console layout.
  </behavior>
  <action>Insert a scheduler control strip into AppShell directly below the existing top bar, matching the UI contract's calm utility-tray treatment. Keep quiet-hours editing and pause-all presets encapsulated inside SchedulerControlStrip and its local subregions rather than spreading them across multiple top-level feature files. Implement quiet-hours editing through a compact inline panel or anchored popover with explicit save, local-time helper copy, and validation that blocks identical start and end times. Implement pause-all through a compact preset menu with exactly the four approved durations and a `Resume now` action when active. Update today overview copy only enough to echo one light app-wide timing fact, keeping primary state ownership in the strip. Do not add a dedicated settings page, dashboard analytics panel, notification controls, history timeline, or per-reminder global control duplication.</action>
  <verify>
    <automated>pnpm vitest run src/features/reminders/components/SchedulerControlStrip.test.tsx -t "pauses reminders"</automated>
  </verify>
  <acceptance_criteria>
    - Quiet hours and pause-all are editable from one dashboard-top strip inside the existing shell.
    - The strip remains visually secondary to the hero cards and primary CTA.
    - Pause-all uses only the four locked presets and resume-now behavior.
  </acceptance_criteria>
  <done>Users can control app-wide timing rules without leaving the main dashboard or entering Phase 4 settings territory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add reminder-level allowed hours in the drawer and surface next-fire constraints lightly</name>
  <files>src/features/reminders/components/ReminderDrawer.tsx, src/features/reminders/components/ReminderDrawer.test.tsx, src/App.css</files>
  <read_first>@.planning/phases/02-scheduling-engine/02-UI-SPEC.md @.planning/phases/02-scheduling-engine/02-VALIDATION.md @src/features/reminders/components/ReminderDrawer.tsx @src/features/reminders/components/ReminderRow.tsx @src/features/reminders/components/NextReminderCard.tsx</read_first>
  <behavior>
    - Test 1: the drawer advanced schedule area can enable or disable allowed hours and validate cross-midnight input.
    - Test 2: allowed-window constraints validate cleanly without introducing dense diagnostics or a second editing surface.
    - Test 3: reminder-level timing editing remains confined to the strip and drawer boundary prepared for 02-03 read-model refresh.
  </behavior>
  <action>Extend the existing drawer advanced schedule section with an `Allowed hours` toggle, start and end inputs, cross-midnight helper copy, and inline validation that blocks identical boundaries. This plan stops at scheduler command wiring, dashboard control strip behavior, drawer editing, and at most one lightweight app-wide timing echo in the existing overview surface. Recovery-driven hero or row status copy, catch-up presentation, and lifecycle invalidation refresh stay out of this plan and move to the recovery hardening plan after reconciliation semantics exist. Keep editing of reminder-level timing inside the drawer only and keep global editing inside the control strip only. Do not add snooze, skip, complete, notification delivery health, missed-event review, or a standalone scheduler page.</action>
  <verify>
    <automated>pnpm vitest run src/features/reminders/components/ReminderDrawer.test.tsx -t "active window"</automated>
  </verify>
  <acceptance_criteria>
    - Reminder-level allowed hours live in the drawer's advanced schedule area and nowhere else.
    - Dashboard read-model surfaces reflect quiet, paused, or allowed-after constraints with one lightweight primary state cue.
    - No Phase 3 delivery UI or Phase 4 settings/history UI leaks into the phase.
  </acceptance_criteria>
  <done>Phase 2 timing rules are visible and editable through the approved dashboard strip and drawer surfaces.</done>
</task>

</tasks>

<verification>
- Run `pnpm vitest run src/features/reminders/form/reminder-form-schema.test.ts` after the typed-boundary task.
- Run `pnpm vitest run src/features/reminders/components/SchedulerControlStrip.test.tsx -t "pauses reminders"` after the dashboard strip task.
- Run `pnpm vitest run src/features/reminders/components/ReminderDrawer.test.tsx -t "active window"` after the drawer task.
- Before moving to 02-03, run `cd src-tauri && cargo test && cd .. && pnpm vitest run` to match the phase validation cadence.
</verification>

<success_criteria>
- Quiet hours, pause-all, and reminder-level allowed hours are exposed through the dashboard strip and existing drawer, not through a settings page.
- Typed frontend and native boundaries remain aligned around scheduler snapshot and schedule payload contracts.
- The plan does not introduce notification delivery, history ledger, settings page, or snooze/skip/complete behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scheduling-engine/02-02-SUMMARY.md`
</output>