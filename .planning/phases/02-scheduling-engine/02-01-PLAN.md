---
phase: 02-scheduling-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/domain/reminder.rs
  - src-tauri/src/domain/schedule.rs
  - src-tauri/src/domain/scheduler.rs
  - src-tauri/src/persistence/reminders.rs
  - src-tauri/src/persistence/scheduler_state.rs
  - src-tauri/src/runtime/scheduler.rs
  - src-tauri/src/commands/reminders.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - SCHD-03
  - SCHD-04
  - DELV-04
must_haves:
  truths:
    - Reminder timing can respect allowed-hour constraints consistently after the app restarts.
    - App-wide timing policy persists as one shared source of truth instead of drifting between reminders.
    - Editing a reminder still results in one coherent next-up ordering rather than fragmented scheduling behavior.
  artifacts:
    - path: src-tauri/src/domain/scheduler.rs
      provides: Quiet-hours, pause, effective-next-due, and runtime-status domain contracts.
    - path: src-tauri/src/persistence/scheduler_state.rs
      provides: Singleton scheduler-state repository for quiet hours, pause-until, and reconciliation markers.
    - path: src-tauri/src/runtime/scheduler.rs
      provides: Derived due-queue worker with explicit rebuild or invalidate entrypoints.
    - path: src-tauri/src/persistence/reminders.rs
      provides: Scheduler-aware reminder refresh path that no longer hides recomputation inside list reads.
  key_links:
    - from: src-tauri/src/persistence/reminders.rs
      to: src-tauri/src/domain/schedule.rs
      via: compiler derives base recurrence and nearest-allowed-time decisions from persisted schedule_json
      pattern: compute_
    - from: src-tauri/src/runtime/scheduler.rs
      to: src-tauri/src/persistence/scheduler_state.rs
      via: queue rebuild reads app-wide quiet-hours and pause state before selecting due items
      pattern: scheduler_state
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/runtime/scheduler.rs
      via: app setup manages one scheduler runtime and seeds the first queue build
      pattern: manage
---

<objective>
Implement the Rust-side schedule compiler and due-queue runtime foundation for Phase 2.

Purpose: Move scheduling authority out of ad hoc repository list refreshes and into explicit native compiler, scheduler-state, and runtime seams that later UI and lifecycle plans can build on.
Output: Extended schedule domain contracts, persisted scheduler_state storage, and a rebuildable due-queue runtime managed from the Tauri app boundary.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scheduling-engine/02-CONTEXT.md
@.planning/phases/02-scheduling-engine/02-RESEARCH.md
@.planning/phases/02-scheduling-engine/02-VALIDATION.md
@.planning/phases/01-reminder-foundation/01-04-SUMMARY.md
@src-tauri/src/domain/schedule.rs
@src-tauri/src/domain/reminder.rs
@src-tauri/src/persistence/reminders.rs
@src-tauri/src/commands/reminders.rs
@src-tauri/src/lib.rs

<interfaces>
Phase 1 leaves the native scheduler boundary here:

```rust
#[tauri::command]
pub fn list_reminders(app: tauri::AppHandle) -> Result<Vec<Reminder>, String>

#[tauri::command]
pub fn save_reminder(app: tauri::AppHandle, reminder: ReminderDraft) -> Result<Reminder, String>
```

and the shared reminder schedule contract here:

```typescript
type ReminderSchedule =
  | { kind: "interval"; everyMinutes: number; anchorMinuteOfDay: number }
  | { kind: "fixed_time"; weekdays: number[]; times: number[] };
```

This plan must extend those contracts without introducing notification delivery, history rows, settings-page state, or snooze/skip/complete semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the native schedule compiler for active-window-aware next-due decisions</name>
  <files>src-tauri/src/domain/mod.rs, src-tauri/src/domain/reminder.rs, src-tauri/src/domain/schedule.rs, src-tauri/src/domain/scheduler.rs</files>
  <read_first>@.planning/phases/02-scheduling-engine/02-CONTEXT.md @.planning/phases/02-scheduling-engine/02-RESEARCH.md @.planning/phases/02-scheduling-engine/02-VALIDATION.md @src-tauri/src/domain/schedule.rs @src-tauri/src/domain/reminder.rs</read_first>
  <behavior>
    - Test 1: a reminder-level active window, including cross-midnight ranges, defers the computed next due to the nearest allowed local time.
    - Test 2: the compiler keeps base recurrence calculation separate from suppression or deferral rules so later plans can layer quiet hours and catch-up policy on top.
    - Test 3: the reminder read model can carry scheduler-facing runtime metadata without leaking notification or history semantics.
  </behavior>
  <action>Extend the Rust schedule domain so `schedule_json` remains the only persisted reminder timing payload while each schedule variant may optionally carry a reminder-level active window. Add native helper types in src-tauri/src/domain/scheduler.rs for local-wall-clock windows, effective next-due decisions, and runtime-status classification that later plans can reuse for quiet hours, pause-all, and recovery. Update src-tauri/src/domain/reminder.rs only enough to carry scheduler-facing read-model fields that belong to Phase 2 foundation. Keep chrono as the authority for local wall-clock math, support cross-midnight windows such as 22:00 to 08:00, and keep the compiler pure and deterministic from `now` plus persisted inputs. Do not add notification delivery, permission state, snooze, skip, complete, history ledgers, or a settings-page model.</action>
  <verify>
    <automated>cd src-tauri && cargo test active_window</automated>
  </verify>
  <acceptance_criteria>
    - The schedule compiler can express both base recurrence and reminder-level allowed-window deferral using Rust-native contracts.
    - `schedule_json` stays authoritative and no frontend-only timing model is introduced.
    - The new domain types are scoped to scheduler decisions, not delivery or history concerns.
  </acceptance_criteria>
  <done>Phase 2 has a Rust-native schedule compiler seam that can be reused by quiet hours, pause-all, and recovery work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist scheduler_state and remove list-time scheduler side effects</name>
  <files>src-tauri/src/persistence/mod.rs, src-tauri/src/persistence/reminders.rs, src-tauri/src/persistence/scheduler_state.rs, src-tauri/src/domain/reminder.rs, src-tauri/src/domain/scheduler.rs</files>
  <read_first>@.planning/phases/02-scheduling-engine/02-RESEARCH.md @.planning/phases/02-scheduling-engine/02-VALIDATION.md @src-tauri/src/persistence/reminders.rs @src-tauri/src/domain/schedule.rs</read_first>
  <behavior>
    - Test 1: the repository can persist and reload one scheduler_state row containing quiet-hours fields, pause-until, and reconciliation markers.
    - Test 2: reminder persistence exposes explicit scheduler refresh methods instead of recomputing `next_due_at` only as a side effect of `list()`.
    - Test 3: the persistence layer stores only the minimum metadata required for Phase 2 runtime decisions.
  </behavior>
  <action>Create src-tauri/src/persistence/scheduler_state.rs as the singleton repository for global quiet-hours and pause-all policy, plus the durable timestamps needed for later lifecycle reconciliation. Update src-tauri/src/persistence/reminders.rs so scheduler refresh happens through explicit compiler-driven methods that accept scheduler context, rather than silently mutating rows during `list()`. Add the minimum schema evolution needed for Phase 2 native scheduling, including any reminder-side columns required for runtime classification, while keeping reminder history, missed-event ledgers, notification attempts, and settings records out of scope. Preserve the current SQLite-first architecture and do not move app-wide timing state into frontend local state or a separate preference system.</action>
  <verify>
    <automated>cd src-tauri && cargo test scheduler_state_repository</automated>
  </verify>
  <acceptance_criteria>
    - Scheduler state is persisted in one native repository with a clear SQL contract.
    - Reminder persistence no longer relies on `list()` as the hidden reconciliation path.
    - The persistence boundary remains Phase-2-sized and excludes delivery, history, and settings-page concerns.
  </acceptance_criteria>
  <done>Quiet hours, pause-all, and later recovery logic now have a durable native storage boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Introduce a rebuildable due-queue runtime and wire reminder mutations into invalidation</name>
  <files>src-tauri/src/runtime/mod.rs, src-tauri/src/runtime/scheduler.rs, src-tauri/src/commands/reminders.rs, src-tauri/src/lib.rs</files>
  <read_first>@.planning/phases/02-scheduling-engine/02-RESEARCH.md @.planning/phases/02-scheduling-engine/02-VALIDATION.md @src-tauri/src/commands/reminders.rs @src-tauri/src/lib.rs @src-tauri/src/persistence/reminders.rs</read_first>
  <behavior>
    - Test 1: the runtime can rebuild an ordered due queue from persisted reminders plus scheduler_state.
    - Test 2: save, delete, and enable-or-disable reminder commands notify the runtime to invalidate or rebuild instead of creating per-reminder timers.
    - Test 3: app startup can manage exactly one scheduler runtime without starting notification delivery or lifecycle recovery logic yet.
  </behavior>
  <action>Create a dedicated runtime module that owns a derived in-memory due queue, rebuilds from persisted reminder and scheduler_state data, and exposes an explicit invalidate-or-rebuild seam for later plans. Wire this runtime into src-tauri/src/lib.rs as managed app state and update src-tauri/src/commands/reminders.rs so reminder CRUD operations can notify the runtime after persistence changes. Keep the runtime strictly Phase 2 foundation: it may classify or queue due reminders, but it must not deliver native notifications, emit history events, or implement snooze/skip/complete behavior. Do not add a settings surface, background agent farm, or multiple competing timer loops.</action>
  <verify>
    <automated>cd src-tauri && cargo test scheduler_runtime</automated>
  </verify>
  <acceptance_criteria>
    - The app owns one rebuildable scheduler runtime backed by persisted data.
    - Reminder mutations feed runtime invalidation through the existing native command boundary.
    - No notification delivery, history, or Phase 4 settings features are introduced.
  </acceptance_criteria>
  <done>The Rust-side scheduler foundation is ready for Phase 2 control surfaces and later lifecycle reconciliation.</done>
</task>

</tasks>

<verification>
- Run `cd src-tauri && cargo test active_window` after the compiler task.
- Run `cd src-tauri && cargo test scheduler_state_repository` after the persistence task.
- Run `cd src-tauri && cargo test scheduler_runtime` after the runtime task.
- Before moving to 02-02, run `cd src-tauri && cargo test` to keep the native boundary green.
</verification>

<success_criteria>
- QuietBloom has a real native scheduling foundation for allowed windows, quiet hours, pause-all, and recovery without leaking into delivery semantics.
- The due queue is derived from persisted state and invalidated through one runtime seam.
- The plan does not introduce notification delivery, history ledger, settings page, or snooze/skip/complete behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scheduling-engine/02-01-SUMMARY.md`
</output>